<section class="articles featured" id="test">
    <div class="container__inner flow">
        <h2 class="section-title text-center tt-upper">Featured Article</h2>
        <hr class="h-rule bgc-secondary-500 m-b-500">
        <ul role="list" class="articles__list flow">
            {%- for item in collections.featured |  limitTo(1) -%}
                {% include '_article-snippet.njk' %}
            {%- endfor -%}
        </ul>
    </div>
</section>