{% set ctaPrefix = cta %}

{% if ctaContent %} 
  {% set ctaPrefix = ctaContent %}
{% endif %}

<section class="cta">
  <div class="container flow">
      <h2 class="cta__heading headline" data-highlight="quaternary">{{ ctaPrefix.title }}</h2>
      <p class="cta__summary measure-short">{{ ctaPrefix.summary }}</p>
      <div class="cta__action">
        <a class="btn btn--primary" data-variant="ghost" href="{{ ctaPrefix.buttonUrl }}">{{ ctaPrefix.buttonText }}</a>
      </div>
  </div>
</section>